<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything; the platform theme only needs NoActionBar.
         System bar appearance is handled by enableEdgeToEdge(). -->
    <style name="Theme.BaseApp" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
